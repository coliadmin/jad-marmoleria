import {api as products} from "@/modules/product";
import {api as colors} from "@/modules/color";
import {api as uses} from "@/modules/use";
import {api as materials} from "@/modules/material";

export const api = {
  products,
  colors,
  uses,
  materials,
};
